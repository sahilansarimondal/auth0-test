import ProfileServer from "@/components/serverProfile";
import React from "react";

const ProfilePage = () => {
  return (
    <div>
      <ProfileServer />
    </div>
  );
};

export default ProfilePage;
